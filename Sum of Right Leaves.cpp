// https://binarysearch.com/problems/Knights'-Attack
#include<bits/stdc++.h>

using namespace std;


class Tree {
    public:
        int val;
        Tree *left;
        Tree *right;
};


int ans;

void recursive(Tree* node, string source) {
    if (!node) return;
    if (!node->left && !node->right && source == )
}

int solve(Tree* root) {
    
}

int main() {
    vector<int> v = {5, 10};
    vector<int> v1 = {2,0,2};
    // solve(v);
    // cout<<solve(v1, 2)<<'\n';
    return 0;
}